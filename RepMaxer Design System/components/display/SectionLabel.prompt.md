Mono caps heading for a card or list, with an optional trailing action.

```jsx
<SectionLabel action={<Button variant="plain" size="sm">Edit</Button>}>Body stats</SectionLabel>
<Divider margin={16} />
```
