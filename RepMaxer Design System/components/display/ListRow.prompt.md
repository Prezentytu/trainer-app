Hairline-separated row — workouts, clients, settings entries.

```jsx
<ListRow title="Push Day A" sub="25 mar · 12 serii · 4 820 kg" right={<Icon name="chevron-right" size={16} />} onClick={open} />
```

Rows sit directly on the background, not inside cards. The last row drops its hairline automatically.
