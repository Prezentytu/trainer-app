Two or three equal choices in a grey well; the active segment is a solid inverted fill.

```jsx
<SegmentedControl value={unit} onChange={setUnit} items={["Metric", "Imperial"]} />
<SegmentedControl value={sex} onChange={setSex} items={["Male", "Female", "Other"]} />
```
