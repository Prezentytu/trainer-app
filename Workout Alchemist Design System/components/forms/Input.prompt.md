Text/number field with caps label, unit suffix and gold focus ring.

```jsx
<Input label="Weight" mono suffix="kg" defaultValue="62.5"/>
<Input label="Plan name" placeholder="Push / pull / legs"/>
<Input label="Reps" error="Enter 1–30"/>
```

Use `mono` for any numeric entry (weights, reps, rest).
