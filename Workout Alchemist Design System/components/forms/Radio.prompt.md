Single radio; group by sharing `name` and switching on `value`.

```jsx
<Radio name="unit" value="kg" label="kg" checked={u==="kg"} onChange={setU}/>
<Radio name="unit" value="lb" label="lb" checked={u==="lb"} onChange={setU}/>
```
