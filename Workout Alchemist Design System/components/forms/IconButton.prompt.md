Square icon-only button for toolbars, list-row actions and app bars.

```jsx
<IconButton label="Add exercise"><PlusIcon/></IconButton>
<IconButton label="Timer" variant="outline" active><TimerIcon/></IconButton>
```

`label` is mandatory. `active` tints gold-dim with a gold icon.
