56px screen bar with no border or blur.

```jsx
<TopBar
  left={<Button variant="plain" size="sm">Anuluj</Button>}
  title="Trening"
  right={<><Button variant="plain" size="sm">Zapisz</Button><Button caps size="sm">Finish</Button></>}
/>
```
