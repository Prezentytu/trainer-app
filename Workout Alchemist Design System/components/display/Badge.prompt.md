Tiny caps status pill. `pr` is the only gold in the product; `positive` = done, `danger` = missed, `accent` = teal highlight.

```jsx
<Badge tone="pr">PR</Badge>
<Badge tone="positive">Done</Badge>
<Badge>Week 3</Badge>
```
