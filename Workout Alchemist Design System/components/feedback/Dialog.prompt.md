Centered confirm dialog over a scrim; 24px radius sheet.

```jsx
<Dialog title="Delete this plan?" description="Maya loses access immediately." danger confirmLabel="Delete plan" onConfirm={fn} onCancel={fn}/>
```
